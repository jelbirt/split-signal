"""Split Likelihood Index: calibrated 12-month split-likelihood scoring.

No expected-return claims — Phase A showed the split event carries no
alpha (docs/METHODOLOGY.md). This package predicts the EVENT only.
"""
